name: Update README cards
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          token: ${{ secrets.GH_PAT }}
          options: username=MickLesk&show_icons=true&theme=tokyonight&count_private=true&include_all_commits=true
          path: profile-stats.svg
      - name: Generate languages card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
          token: ${{ secrets.GH_PAT }}
          options: username=MickLesk&layout=compact&theme=tokyonight
          path: profile-langs.svg
      - name: Commit cards
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add *.svg
          git commit -m "Update README cards with PAT" || exit 0
          git push
